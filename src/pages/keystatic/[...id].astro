---
import { Keystatic } from '@keystatic/astro/ui';

// 1. Force Astro to render this route dynamically on the server at runtime
export const prerender = false;
---

<Keystatic client:only="react" />