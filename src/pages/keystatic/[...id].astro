---
import { Keystatic } from '@keystatic/astro/ui';

// Force Vercel to use standard Node.js serverless functions for this route
export const config = {
  runtime: 'nodejs20.x'
};
---

<Keystatic client:only="react" />